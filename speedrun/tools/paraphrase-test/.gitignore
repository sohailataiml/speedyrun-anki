# Regenerated by study_order.py each run - a binary Anki collection, not
# evidence (the evidence is study_orders.json's extracted queue orders).
output/fixture/

# Pure API-response cache keyed by prompt hash, purely mechanical and
# fully redundant with ablation_results.json/sufficiency_results.json -
# not worth committing.
output/run_cache.json
